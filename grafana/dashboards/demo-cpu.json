{
  "title": "Demo Service + CPU Exporter (Test)",
  "panels": [
    {
      "type": "timeseries",
      "title": "CPU usage % (1m)",
      "gridPos": { "x": 0, "y": 0, "w": 12, "h": 8 },
      "targets": [
        { "expr": "100 * (1 - rate(cpu_exporter_cpu_usage_seconds_total{mode=\"idle\"}[1m]))" }
      ],
      "fieldConfig": { "defaults": { "unit": "percent", "decimals": 1 }, "overrides": [] }
    }
  ],
  "timezone": "",
  "time": { "from": "now-1h", "to": "now" },
  "schemaVersion": 39,
  "version": 1
}
